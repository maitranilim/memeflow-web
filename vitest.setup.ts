import '@testing-library/jest-dom/vitest';

// jsdom doesn't implement matchMedia; Framer Motion checks it for
// reduced-motion support.
if (!window.matchMedia) {
  window.matchMedia = (query: string) => ({
    matches: false,
    media: query,
    onchange: null,
    addListener: () => {},
    removeListener: () => {},
    addEventListener: () => {},
    removeEventListener: () => {},
    dispatchEvent: () => false,
  });
}
